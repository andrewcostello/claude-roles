project: claude-workflow
epic: Effort study — is reasoning cheaper to buy on a cheap model?
target_repo: claude-workflow
tasks:
  - key: EF-SCAFFOLD
    summary: scaffold — GO-1-1s wiring contract, landed and panel-approved
    description: "ALREADY DONE. Exists so each arm can name a scaffold in blockedBy,\nwhich PO-1 requires. The contract is GO-1-1's, landed at b0313fa with\nunanimous panel approval; every arm forks from it.\n"
    type: Task
    estimate: 0h
    labels:
      - size:XS
      - matrix
    agent: claude
    role: scaffold
    status: Done
  - key: EF-HAIKU-LOW-1
    summary: effort low 1/3 — claude/claude-haiku-4-5-20251001
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-haiku-4-5-20251001
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-HAIKU-LOW-2
    summary: effort low 2/3 — claude/claude-haiku-4-5-20251001
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-haiku-4-5-20251001
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-HAIKU-LOW-3
    summary: effort low 3/3 — claude/claude-haiku-4-5-20251001
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-haiku-4-5-20251001
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-HAIKU-HIGH-1
    summary: effort high 1/3 — claude/claude-haiku-4-5-20251001
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-haiku-4-5-20251001
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-HAIKU-HIGH-2
    summary: effort high 2/3 — claude/claude-haiku-4-5-20251001
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-haiku-4-5-20251001
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-HAIKU-HIGH-3
    summary: effort high 3/3 — claude/claude-haiku-4-5-20251001
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-haiku-4-5-20251001
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-SONNET-LOW-1
    summary: effort low 1/3 — claude/claude-sonnet-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-sonnet-5
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-SONNET-LOW-2
    summary: effort low 2/3 — claude/claude-sonnet-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-sonnet-5
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-SONNET-LOW-3
    summary: effort low 3/3 — claude/claude-sonnet-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-sonnet-5
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-SONNET-HIGH-1
    summary: effort high 1/3 — claude/claude-sonnet-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-sonnet-5
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-SONNET-HIGH-2
    summary: effort high 2/3 — claude/claude-sonnet-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-sonnet-5
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-SONNET-HIGH-3
    summary: effort high 3/3 — claude/claude-sonnet-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-sonnet-5
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-OPUS-LOW-1
    summary: effort low 1/3 — claude/claude-opus-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-opus-5
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-OPUS-LOW-2
    summary: effort low 2/3 — claude/claude-opus-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-opus-5
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-OPUS-LOW-3
    summary: effort low 3/3 — claude/claude-opus-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-opus-5
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-OPUS-HIGH-1
    summary: effort high 1/3 — claude/claude-opus-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-opus-5
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-OPUS-HIGH-2
    summary: effort high 2/3 — claude/claude-opus-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-opus-5
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-OPUS-HIGH-3
    summary: effort high 3/3 — claude/claude-opus-5
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: claude
    model: claude-opus-5
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-CODEX-LOW-1
    summary: effort low 1/3 — codex/gpt-5.6-sol
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: codex
    model: gpt-5.6-sol
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-CODEX-LOW-2
    summary: effort low 2/3 — codex/gpt-5.6-sol
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: codex
    model: gpt-5.6-sol
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-CODEX-LOW-3
    summary: effort low 3/3 — codex/gpt-5.6-sol
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: codex
    model: gpt-5.6-sol
    effort: low
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-CODEX-HIGH-1
    summary: effort high 1/3 — codex/gpt-5.6-sol
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: codex
    model: gpt-5.6-sol
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-CODEX-HIGH-2
    summary: effort high 2/3 — codex/gpt-5.6-sol
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: codex
    model: gpt-5.6-sol
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
  - key: EF-CODEX-HIGH-3
    summary: effort high 3/3 — codex/gpt-5.6-sol
    description: "EFFORT STUDY. Four models spanning the cost range, both ends of the\neffort knob, three runs of each cell.\n\nTHE QUESTION, and it is the sharpest cost question available: can a CHEAP model\nat HIGH effort beat an EXPENSIVE one at LOW effort — on quality, on wall-clock,\nand on dollars? If it can, the routing rule is not \"use the best model\", it is\n\"buy reasoning where it is cheapest\", which is a different and much more\nactionable conclusion.\n\nWHY IT IS ITS OWN STUDY. Effort was originally left uncontrolled — codex and\ngrok were pinned high while the claude-routed arms took the CLI default — which\nmade it a second variable riding inside a model comparison and would have made\nany difference unattributable. It is worth measuring; it is not worth\nconfounding the replicate study with. That one now holds effort constant at\nhigh, and this one varies it deliberately.\n\nScored by features/model-matrix/scorecard.py: mutation kill rate over nine\ndefects, gate state, cost, wall-clock. No panel — first-pass output only, which\nis the same scope as the replicate study so the two are comparable.\n\n--- THE BRIEF ---\n\nSeal GO-1-1's contract. The row that matters FAILS today and its exact\nmutation is already measured: `emit()`'s `ContractV2` arm rewritten to\n`EmitV1` must redden this file, and at `0cfdb57` it reddens nothing.\n\nCover the mapping, not the library: (contract x -out x -json) -> artifact\nset + exit code. Include `-contract-version 3` -> exit 3 with a message\nnaming the accepted set, and `-contract-version 2` producing the wrapper\non stdout.\n\nA LIVE VACUOUS SEAL IS IN SCOPE AND IS MEASURED.\n`TestSeal_InstalledDigestSource_YieldsHexOrErrors`\n(`capability_seal_test.go:409`) returns early on error having asserted\nnothing about the values. Proved rather than argued: running that test\nALONE under coverage gives `ConsumedDigests` 90.0% and `hexSHA256` 0.0% —\nthe hex/length/lowercase assertions below the early return never execute,\nbecause `ConsumedDigests` raises unless BOTH `sawConfig` and `sawDiff`\nare set, and no test in the package calls `readDiff`. Say whether it is\namended or struck.\n\nTWO CORRECTIONS TO THE RECORD, both making the unit SMALLER, both\nmeasured at `0cfdb57` and both to be re-derived rather than trusted:\n\n  1. \"Swapping the digests passes the suite\" NO LONGER REPRODUCES.\n     Rewriting `ConsumedDigests`' return from\n     `hexSHA256(s.config), hexSHA256(s.diff)` to the reverse reddens\n     EXACTLY ONE row —\n     `TestSeal_Repair_ResolveConfigDual_ConsumedBytesMustBeTheCertifiedBytes`,\n     added by a later repair wave. The swap is caught. Do not write a\n     row for a defect that is already sealed.\n  2. \"The production DigestSource success path never executes in any\n     test\" is CLOSED. Whole-suite coverage at `0cfdb57`: `recordConfig`\n     100.0%, `recordDiff` 100.0%, `ConsumedDigests` 100.0%, `hexSHA256`\n     100.0%. What remains is that ONE seal's own assertions are\n     unreachable — a narrower defect than the one recorded, and it is the\n     one to seal.\n\nNON-VACUITY: this module has already produced four measured vacuous\nseals. Do not seal that a function was CALLED; seal what it ANSWERED.\nProve each row red by the specific defect it names and carry an in-test\ncontrol judged in the same call."
    type: Task
    estimate: 3h
    labels:
      - size:L
      - area:go-tools
      - effort-study
    agent: codex
    model: gpt-5.6-sol
    effort: high
    role: seals
    status: To Do
    blockedBy:
      - EF-SCAFFOLD
base_branch: b0313fa
_base_note: Pinned to a SHA, not a branch. feat/GO-1-1-... moved 30+ times during the 2026-09-02 runs, so a re-run against the branch forks from a different tree and is not comparable. b0313fa is the tree every arm in those runs actually saw.
